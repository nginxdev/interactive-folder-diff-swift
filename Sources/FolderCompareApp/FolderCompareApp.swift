import SwiftUI

/// The entry point for the "Interactive folder diff" application.
@main
struct FolderCompareApp: App {
    var body: some Scene {
        WindowGroup("Interactive folder diff") {
            ContentView()
        }
    }
}
